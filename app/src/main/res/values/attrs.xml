<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="FixurePositionProgressBar">
        <!-- 设置进度条的进度属性 -->
        <attr name="progress_current" format="integer" />
        <attr name="progress_max" format="integer" />
        <!-- 设置进度条的背景色 -->
        <attr name="progress_unreached_color" format="color" />
        <attr name="progress_reached_color" format="color" />
        <!-- 设置进度条的高度 -->
        <attr name="progress_reached_bar_height" format="dimension" />
        <attr name="progress_unreached_bar_height" format="dimension" />
        <!-- 设置预设的进度条可播放位置 -->
        <attr name="progress_fixure_position" format="integer" />
        <attr name="progress_fixure_color" format="color" />
        <!-- 设置进度条预设位置显示不显示 -->
        <attr name="progress_fixure_position_visibility" format="enum">
            <enum name="visible" value="0" />
            <enum name="invisible" value="1" />
        </attr>
    </declare-styleable>

    <declare-styleable name="FixureProgressBar">
        <attr name="progress_fixure_pb" format="integer"/>
        <attr name="progress_fixure_color_pb" format="color"/>
    </declare-styleable>

    <declare-styleable name="CircularPercentagView">
        <attr name="stroke_width" format="integer"/>
        <attr name="start_color" format="string"/>
        <attr name="end_color" format="string"/>
        <attr name="text_size" format="integer"/>
    </declare-styleable>

    <!--wavesWidth：一个水波纹的宽度-->
    <!--wavesCount：有几个水波纹-->
    <!--wavesColor：水波纹的颜色（最深的那个颜色）-->
    <!--centerDiameter：中心图片的直径。中心图片一定是一个原型图片-->
    <!--wavesAutoRunning：水波纹是否自动做动画-->
    <!--runningTime：做动画的间隔时间，单位是毫秒-->
    <!--runningStep：水波纹每次变化的增加的幅度。值越大，动画越快-->

    <declare-styleable name="WaterRippleView">
        <attr name="wavesWidth" format="dimension|reference"/>
        <attr name="wavesCount" format="integer|reference"/>
        <attr name="wavesColor" format="color|reference"/>
        <attr name="centerDiameter" format="integer"/>
        <attr name="wavesAutoRunning" format="boolean"/>
        <attr name="runningTime" format="integer"/>
        <attr name="runningStep" format="integer"/>
    </declare-styleable>
</resources>